(require 'core (concat user-emacs-directory "core/core"))

(asdf/init
  ;crux
  company
  ;docker
  deft
  doom-theme
  eglot
  evil
  folding
  magit
  markdown
  ;popwin
  ;powershell
  tree-sitter
  treemacs
  ;dashboard
  ;github-theme  
  doom-modeline
  yaml
  )
