(require 'core (concat user-emacs-directory "core/core"))

(asdf/init
  ;crux
  company
  ;docker
  doom-theme
  eglot
  ;folding
  magit
  markdown
  ;popwin
  ;powershell
  tree-sitter
  treemacs
  ;dashboard

  ;github-theme  
  ;doom-modeline
  )
